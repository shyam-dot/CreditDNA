# Minimal requirements for Vercel Serverless Python Functions
# This file is intentionally stripped of all heavy ML/data science packages
# (xgboost, scikit-learn, shap, joblib, numba, llvmlite) which exceed
# Vercel's 250MB function size limit and require native compilation.
# The backend falls back to deterministic formula-based scoring on Vercel.
fastapi>=0.111.0
uvicorn>=0.30.1
pydantic>=2.7.1
pydantic-settings>=2.3.0
firebase-admin>=6.5.0
python-jose>=3.3.0
httpx>=0.27.0
python-multipart>=0.0.9
numpy>=1.26.4
requests>=2.32.3
python-dotenv>=1.0.1
